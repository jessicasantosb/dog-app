import React from 'react'
import styles from './Home.module.css'

function Home() {
  return (
    <section className={`${styles.home} container`}>
      home
    </section>
  )
}

export default Home
